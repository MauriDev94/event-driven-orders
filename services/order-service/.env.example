# order-service — local environment. Copy to .env for local (non-docker) runs.
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
DB_HOST=localhost
DB_PORT=5433
DB_USER=orders
DB_PASSWORD=orders
DB_NAME=orders
