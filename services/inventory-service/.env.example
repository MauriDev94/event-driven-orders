# inventory-service — local environment. Copy to .env for local (non-docker) runs.
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
DB_HOST=localhost
DB_PORT=5434
DB_USER=inventory
DB_PASSWORD=inventory
DB_NAME=inventory
